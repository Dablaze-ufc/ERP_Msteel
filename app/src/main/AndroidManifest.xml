<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="example.com.erp">
    <!-- Remove due to google policy -->
    <!--
<uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    -->

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utility.AppController"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="m">
        <activity android:name=".activity.BillReceiptActivity">

        </activity>
        <activity android:name=".activity.ChallanDetailActivity" />
        <activity android:name=".activity.ChallanStatmentActivity" />
        <activity android:name=".activity.EnquiryActivity" />

        <activity
            android:name=".activity.SplashScreenActivity"
            android:configChanges="orientation"
            android:theme="@style/AppTheme.ActLogin">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme.Home" />

        <activity
            android:name=".activity.AuthCodeVerificationActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.Home" />

        <activity
            android:name=".activity.SubItemActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.SubSubItemActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.GraphActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.CartItemActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.TransactionDetailsActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.OrderDetailsActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.OrderableItemActivity"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.ActHandlePushService"
            android:theme="@style/AppTheme.Home" />
        <activity
            android:name=".activity.EditProfileActivity"
            android:theme="@style/AppTheme.Home" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="example.com.erp"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path"
                tools:replace="android:resource" />
        </provider>
        <receiver android:name=".utility.ConnectivityReceiver">
            <intent-filter>
                <action
                    android:name="android.net.conn.CONNECTIVITY_CHANGE"
                    tools:ignore="BatteryLife" />
            </intent-filter>
        </receiver>

        <service android:name=".utility.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service android:name=".utility.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>